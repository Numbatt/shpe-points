.DS_Store
node_modules/
.env
.env.*
!.env.example

# Legacy exports contain member PII (netIDs, emails, birthdays).
# Kept locally as a safety net before schema changes; never committed.
scripts/legacy-export/*.csv
.gstack/

# Per-machine Claude Code settings.
.claude/settings.local.json

# Supabase CLI local state. Records which project this checkout is linked to,
# which is per-machine and not the same thing as the project ref in .mcp.json.
supabase/.temp/
.vercel
.env*
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
